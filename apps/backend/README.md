# Backend (Django + DRF)

Not yet implemented. This is Phase 1+ work.

Target module layout when implementation begins (see
`docs/architecture/DATA_MODEL.md` Section 1):

```
apps/backend/
    config/            # Django project settings (base/dev/prod), urls, celery app
    accounts/
    organizations/
    permissions/
    workspaces/
    storage/
    databases/
    datasets/
    imports/
    applications/
    sharing/
    audit/
    system/
    manage.py
    pyproject.toml / requirements/
```

Do not add code here until the corresponding ROADMAP phase is underway.
