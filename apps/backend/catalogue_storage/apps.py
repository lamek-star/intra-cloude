from django.apps import AppConfig


class CatalogueStorageConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "catalogue_storage"
    verbose_name = "Catalogue Storage (OEM parts / diagrams)"
