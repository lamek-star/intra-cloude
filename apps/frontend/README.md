# Frontend (TypeScript + React + Next.js)

Not yet implemented. This is Phase 1+ work.

Target top-level structure when implementation begins (see ADR-0007):

```
apps/frontend/
    app/ or pages/     # Next.js routes
    components/         # Shared, accessible component library
    lib/                  # API client (generated from OpenAPI where possible), auth helpers
    styles/
    package.json
    tsconfig.json
```

Do not add code here until the corresponding ROADMAP phase is underway.
