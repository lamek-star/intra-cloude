name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  backend:
    name: Backend (lint, typecheck, test)
    runs-on: ubuntu-latest
    services:
      postgres-control:
        image: postgres:18
        env:
          POSTGRES_DB: pdc_control_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres" --health-interval 5s
          --health-timeout 5s --health-retries 10
      postgres-tenant:
        image: postgres:18
        env:
          POSTGRES_DB: pdc_tenant_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5433:5432
        options: >-
          --health-cmd "pg_isready -U postgres" --health-interval 5s
          --health-timeout 5s --health-retries 10
      valkey:
        image: valkey/valkey:9-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "valkey-cli ping" --health-interval 5s
          --health-timeout 5s --health-retries 10
    env:
      CONTROL_DB_HOST: localhost
      CONTROL_DB_PORT: 5432
      TENANT_DB_HOST: localhost
      TENANT_DB_PORT: 5433
      REDIS_URL: redis://localhost:6379/0
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: pip install -r apps/backend/requirements/dev.txt

      - name: Ruff
        working-directory: apps/backend
        run: ruff check .

      - name: Mypy
        working-directory: apps/backend
        run: mypy .

      - name: Pytest
        # Run from the repo root (not apps/backend) so tests/security/
        # (cross-app tenant-isolation tests) are collected alongside each
        # app's own tests/ — see pytest.ini at the repo root.
        run: pytest --cov=apps/backend --cov-report=term-missing

  frontend:
    name: Frontend (lint, typecheck, build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: apps/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx tsc --noEmit

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
        env:
          NEXT_TELEMETRY_DISABLED: "1"
