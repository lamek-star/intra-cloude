# Reverse Proxy Configuration

Not yet implemented. Phase 1 will add the reverse proxy configuration
(Caddy or nginx — final choice recorded via ADR) implementing TLS
termination, routing to `frontend`/`backend`, and basic security headers /
rate limiting, per `docs/deployment/LOCAL_DEPLOYMENT.md` and
`docs/architecture/ARCHITECTURE.md` Section 4.
