# Dependency Version Decisions

Status: Phase 1 baseline
Recorded: 2026-08-07 (verified against current release status as of this
date, per Section 27 of the master prompt — "determine currently supported
stable/LTS versions available in the development environment" before
selecting major dependencies)

This document exists so future work doesn't have to re-derive *why* a
given major version was picked. Exact patch versions are pinned in lock
files (`apps/backend/requirements/*.txt`, `apps/frontend/package-lock.json`)
and are expected to drift forward within a major/minor line as those lock
files are refreshed; this table tracks the deliberate major-version calls.

| Dependency | Chosen | Rationale |
|---|---|---|
| Python | 3.13 | 3.14 is newest, but 3.13 has a longer track record and is explicitly confirmed compatible with DRF 3.16/Django 5.2 LTS at time of writing; re-evaluate 3.14 once the ecosystem (esp. C-extension wheels) has caught up. |
| Django | 5.2 LTS | LTS branch (supported ~3 years from release), not the newer 6.1 standard release — this platform is meant to hold real organizational data long-term, and LTS minimizes forced major upgrades. Revisit when 5.2 approaches EOL or a compelling 6.x LTS ships. |
| Django REST Framework | `~=3.16` constraint, resolved to 3.18.0 at lock time | Confirmed compatible with Django 5.2 LTS; DRF ships fast-moving minor releases, so the `requirements/base.in` floor is 3.16 and `requirements/base.txt` pins the actual resolved 3.18.0 (see `apps/backend/requirements/`). |
| Celery | 5.6.x (resolved 5.6.3) | Current stable line. |
| **Valkey** (replaces "Redis" in ADR-0010) | 9.x (client library: `redis-py` 5.3.1, wire-compatible) | See [ADR-0011](adr/0011-valkey-over-redis.md) — BSD-3 licensed, wire-compatible Redis fork; avoids Redis 8's AGPLv3 copyleft terms for infrastructure the product ships to self-hosters. |
| PostgreSQL | 18 | Latest major (released Sept 2025), stable for ~11 months at time of writing; PostgreSQL 19 is in beta and not production-appropriate yet. |
| Node.js | 24 (Active LTS) | Active LTS supported through April 2028; Node 26 is the newer "Current" line but doesn't enter LTS until October 2026 — not yet the conservative choice for a production frontend build. Matches the Node version already present in this dev environment (v24.18.0). |
| Next.js | 16.x (Active LTS branch), resolved 16.3.0 via `create-next-app@latest` at scaffold time | Current stable major, in Active LTS status since October 2025. |
| React | 19.x, resolved 19.2.8 | Version Next.js 16.3 targets. |
| TypeScript | `^5` per Next.js 16.3's own template (resolved 5.9.3) | TypeScript 7.0 (the new Go-based compiler) shipped only days before this decision (2026-08-05) — deliberately not force-upgraded to for a new production codebase until the broader tooling ecosystem (Next.js, ESLint plugins, editor integrations) confirms full compatibility. Deferred to `create-next-app`'s own judgment on the compatible 5.x line rather than overriding it. Revisit in a later phase. |
| MinIO | latest stable (pin exact tag at Phase 3 implementation) | S3-compatible local object storage per ADR-0004; not yet pinned to a specific tag since object-storage code doesn't exist until Phase 3 — `docker-compose.yml` uses a floating tag with a note to pin before production use. |
| Caddy | latest stable (pin exact tag before production use) | Reverse proxy; automatic HTTPS support is convenient for the internet-gateway phase (Phase 10) even though local/LAN use starts with internal/self-signed certs. |

## Verification

Nothing in this table is asserted from memory. Backend:
`apps/backend/requirements/*.txt` were generated by actually resolving
`requirements/*.in` against live PyPI on 2026-08-07 (via `uv pip compile`),
and the resulting environment was used to run `python manage.py check`,
`pytest --collect-only`, `ruff check .`, and `mypy .` against the real
Phase 1 backend scaffold — all passed. Frontend: scaffolded with
`create-next-app@latest` against the live npm registry on 2026-08-08,
which resolved the exact versions above; `npx tsc --noEmit`, `npm run
lint`, and `npm run build` (including the `output: "standalone"` Docker
target) were all run against the real scaffold and passed. Infrastructure
image tags (PostgreSQL/Valkey/MinIO/Caddy) were checked via web search on
the same dates but not locally pulled/run, since this development
environment does not have Docker available — verify `docker compose build`
end-to-end as an early Phase 2 step.

## Process for Future Version Decisions

1. Before adopting or upgrading a major dependency, check its actual
   current release/support status (not assumed from training knowledge or
   from this document if it has aged).
2. Avoid unsupported/EOL releases.
3. Record the choice here (update the table) and, if the choice is
   architecturally significant (changes a technology, not just a patch
   bump), add an ADR.
4. Use lock files; do not hand-edit generated lock files.
5. Do not upgrade a major dependency version during unrelated feature work
   without separately assessing compatibility.
