<UserControl x:Class="IntraCloud.ControlCenter.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Service: " VerticalAlignment="Center" Margin="0,0,4,0" />
            <ComboBox ItemsSource="{Binding Services}" SelectedItem="{Binding SelectedService}" Width="140" Margin="0,0,12,0" />
            <TextBlock Text="Tail lines: " VerticalAlignment="Center" Margin="0,0,4,0" />
            <TextBox Text="{Binding TailLines, UpdateSourceTrigger=PropertyChanged}" Width="60" Margin="0,0,12,0" />
            <Button Content="Fetch Logs" Command="{Binding FetchLogsCommand}" Margin="0,0,8,0" Padding="12,4" />
            <Button Content="Collect Diagnostics" Command="{Binding CollectDiagnosticsCommand}" Padding="12,4" />
        </StackPanel>

        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" TextWrapping="Wrap" Margin="0,0,0,8" Foreground="Gray" />

        <TextBox Grid.Row="2" Text="{Binding LogText, Mode=OneWay}" IsReadOnly="True"
                 FontFamily="Consolas" FontSize="11" TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
    </Grid>
</UserControl>
